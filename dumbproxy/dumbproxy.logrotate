/var/log/lounge/nginx-error.log /var/log/lounge/nginx/access.log {
	rotate 5
	compress
	daily
	notifempty
	missingok
	sharedscripts
	postrotate
		kill -HUP $( cat /var/run/lounge/nginx.pid ) || true
	endscript
}
